import { LightningElement } from 'lwc';

export default class CpqQuoteInsights extends LightningElement {
    handleReview(event) {
        event.preventDefault();
        // Placeholder for roadmap review
    }
}
